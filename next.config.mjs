/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.pexels.com', 'dynamic-media-cdn.tripadvisor.com', 'static.wixstatic.com'],
  },
};

export default nextConfig;